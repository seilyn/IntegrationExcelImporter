<UserControl x:Class="IntegrationExcelImporter.View.FileImportGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IntegrationExcelImporter.View"     
             mc:Ignorable="d" >

    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding Files, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="파일명" Binding="{Binding FileName}"/>
                <DataGridTextColumn Header="확장자" Binding="{Binding FileExtension}"  />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
